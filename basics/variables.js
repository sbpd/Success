// You can't define a variable more than once

let  petName = 'Lalu'

petName = 'Spot'

console.log(petName)

// There are rules related to variable names
/**
 * The first letter in variable needs to start a-z, $ or _. It can't start with a nuumber
 * Variable names cannot be reserved keywords
 */

 
